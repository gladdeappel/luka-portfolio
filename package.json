{
  "name": "my-v0-project",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "install": "rm -rf node_modules && npm install --production=false --force",
    "lint": "next lint"
  },
  "dependencies": {
    // Je originele dependencies (zonder wijzigingen)
    "@emotion/is-prop-valid": "latest",
    "@heroicons/react": "latest",
    // ... (al je bestaande dependencies blijven staan) ...
    "next": "15.2.4",
    "react": "^19",
    "react-dom": "^19",
    "typescript": "^5",
    "tailwindcss": "^3.4.17",
    "postcss": "^8.5"
  },
  "devDependencies": {
    // Verplaats ALLE build-noodzakelijke packages naar dependencies
    // Alleen typings blijven hier:
    "@types/node": "^22",
    "@types/react": "^19",
    "@types/react-dom": "^19"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
